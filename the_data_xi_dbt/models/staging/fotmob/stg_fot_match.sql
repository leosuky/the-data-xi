/* trivial tier (flat): RAW already typed -> passthrough + uids. */
select
    s.fotmob_id,
    s.combo_id,
    s.match_name,
    s.match_round,
    s.league_id,
    s.league_name,
    s.league_round_name,
    s.country_code,
    s.home_team_id,
    s.home_team_name,
    s.home_score,
    s.away_team_id,
    s.away_team_name,
    s.away_score,
    s.match_date,
    s.stadium_name,
    s.stadium_city,
    s.stadium_country,
    s.stadium_capacity,
    s.attendance,
    s.referee_name,
    s.referee_country,
    s.motm_id,
    s.motm_name,
    s.motm_rating,
    s.ht_coach_id,
    s.ht_coach_name,
    s.at_coach_id,
    s.at_coach_name,
    s.home_color,
    s.away_color,
    s.home_color_dark,
    s.away_color_dark,
    s.home_font_color,
    s.away_font_color,
    s.home_font_dark,
    s.away_font_dark,
    s.has_shot_data,
    s.has_momentum_data
from {{ source('raw','fot_match') }} s
